import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  // Replace with your GitHub Pages URL, e.g. 'https://username.github.io'
  // or 'https://username.github.io/repo-name' if deploying to a sub-path
  site: 'https://tspringborg.github.io',
});
